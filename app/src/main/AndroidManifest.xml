<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a360ads.eshare" >

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".EshareApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <!--启动页面-->
        <activity android:name=".activitys.FirstActivity"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--广告界面-->
        <activity android:name=".activitys.AdActivity"
            android:screenOrientation="portrait"/>

        <!--登录界面-->
        <activity android:name=".activitys.LoginActivity"
            android:screenOrientation="portrait"/>

        <!--新手向导界面-->
        <activity android:name=".activitys.GuideActivity"
            android:screenOrientation="portrait"/>

        <!--基础的webActivity-->
        <activity android:name=".activitys.EwebActivity"
            android:screenOrientation="portrait"/>

        <!--修改密码-->
        <activity android:name=".activitys.AlterPwdActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>
